# FederalRunner Cloud Run Deployment Configuration
# Copy this file to .env.deployment and fill in your values

# ============================================
# Google Cloud Configuration
# ============================================
PROJECT_ID=your-gcp-project-id
REGION=us-central1
SERVICE_NAME=federalrunner-mcp

# ============================================
# Auth0 Configuration
# ============================================
# CRITICAL: AUTH0_ISSUER must end with a trailing slash!
AUTH0_DOMAIN=your-tenant.us.auth0.com
AUTH0_ISSUER=https://your-tenant.us.auth0.com/

# Note: AUTH0_API_AUDIENCE and MCP_SERVER_URL will be set automatically
# to the deployed Cloud Run URL after first deployment

# ============================================
# Cloud Run Resource Configuration
# ============================================
MEMORY=2Gi
CPU=2
TIMEOUT=300
MIN_INSTANCES=0
MAX_INSTANCES=10

# Cloud Run Timeout Notes:
# - Must be >= FEDERALRUNNER_EXECUTION_TIMEOUT (240s)
# - FSA navigation can take up to 180s (extremely variable)
# - 300s = 240s execution + 60s buffer
# - Maximum allowed: 300s (5 minutes) for Cloud Run 1st gen

# CPU/Memory valid combinations:
#   1Gi → max 1 CPU
#   2Gi → max 2 CPUs  (recommended for FederalRunner)
#   4Gi → max 4 CPUs
#   8Gi → max 8 CPUs
