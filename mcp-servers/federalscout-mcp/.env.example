# FederalScout MCP Configuration
# Copy this file to .env and customize as needed

# Browser Settings
FEDERALSCOUT_BROWSER_TYPE=webkit
FEDERALSCOUT_HEADLESS=false
FEDERALSCOUT_SLOW_MO=500

# Demo Mode: Connect to existing browser (for screen recording)
# When enabled, FederalScout connects to an existing browser instead of launching a new one
# This allows you to pre-position the browser window for clean screen recordings
#
# To use demo mode:
#   1. Run: python scripts/start_browser_for_demo.py
#   2. Position browser window exactly where you want it (e.g., right side of screen)
#   3. Run pytest or start Claude Desktop
#   4. FederalScout will connect to the existing browser (no window movement!)
#
# IMPORTANT: Use 127.0.0.1 instead of localhost to avoid IPv6 connection issues on macOS
#
# Uncomment to enable demo mode:
# FEDERALSCOUT_BROWSER_ENDPOINT=http://127.0.0.1:9222
# FEDERALSCOUT_BROWSER_TYPE=chromium

# Viewport Settings (for screen recording)
# Adjust these to fit your recording layout
FEDERALSCOUT_VIEWPORT_WIDTH=1000
FEDERALSCOUT_VIEWPORT_HEIGHT=1000

# Screenshot Settings
FEDERALSCOUT_SCREENSHOT_QUALITY=60
FEDERALSCOUT_SCREENSHOT_MAX_SIZE_KB=50
FEDERALSCOUT_SAVE_SCREENSHOTS=true

# Session Settings
FEDERALSCOUT_SESSION_TIMEOUT=1800

# Directory Settings (optional - intelligent defaults used)
# FEDERALSCOUT_WORKSPACE_ROOT=/path/to/federalscout-mcp
# FEDERALSCOUT_WIZARDS_DIR=../../wizards  # SHARED: multi-agent-federal-form-automation-system/wizards/ (default)
# FEDERALSCOUT_LOG_DIR=/path/to/federalscout-mcp/logs
# FEDERALSCOUT_SCREENSHOT_DIR=../../wizards/screenshots/federal-scout  # SHARED: FederalScout discovery screenshots
#
# IMPORTANT: Wizards and screenshots directories are SHARED
#   multi-agent-federal-form-automation-system/wizards/wizard-structures/  - Wizard Structure JSON files (version controlled)
#   multi-agent-federal-form-automation-system/wizards/data-schemas/       - User Data Schemas (version controlled)
#   multi-agent-federal-form-automation-system/wizards/screenshots/federal-scout/  - FederalScout discovery screenshots
#   mcp-servers/federalscout-mcp/logs/            - FederalScout application logs

# For screen recording configurations:
#
# Split-screen (Claude Desktop left, Browser right):
#   FEDERALSCOUT_VIEWPORT_WIDTH=1200
#   FEDERALSCOUT_VIEWPORT_HEIGHT=1400
#
# Taller viewport (fit more fields):
#   FEDERALSCOUT_VIEWPORT_WIDTH=1200
#   FEDERALSCOUT_VIEWPORT_HEIGHT=1600
#
# Full-screen browser:
#   FEDERALSCOUT_VIEWPORT_WIDTH=1600
#   FEDERALSCOUT_VIEWPORT_HEIGHT=1400
